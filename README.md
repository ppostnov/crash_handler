crash_handler
=============

A cross-platform C++ crash handling library.

PROJECT OVERVIEW
Scons is used to build the project. Compiler should support C++11.
ExVim is used as an IDE, so there's a crash_handler.vimentry file.
